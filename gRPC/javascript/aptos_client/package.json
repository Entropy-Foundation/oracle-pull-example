{
  "name": "pull_service_client",
  "version": "1.0.0",
  "description": "I've created a JavaScript library that streamlines gRPC requests to acquire Oracle proofs. These Oracle proofs are then put to use within the main.js file, where an illustrative example showcases how to integrate them. Users have the flexibility to customize the callContract function in the main.js file according to their specific needs.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grpc/grpc-js": "^1.9.0",
    "@grpc/proto-loader": "^0.7.8",
    "aptos": "^1.20.0"
  }
}
